'use strict';
// De para da chamada do components para o Models usado
// nome do components : model
const user = require('../mongoose/organism/organism-user');

module.exports = ({
    users: user
})
